import re
import ast

def rewrite():
    with open("modules/evaluator.py", "r") as f:
        lines = f.readlines()
    
    # We will let an ai agent do the rewrite by using replace_string_in_file?
    pass
